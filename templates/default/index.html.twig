{% extends 'base.html.twig' %}

{% block body %}
	{{ form_start(form) }}
		{{ form_widget(form) }}
	{{ form_end(form) }}

	{% if (value_to_broadcast|default(false)) %}
		<iframe style="width: 100%; height: 30em;" src="{{ url('broadcast', {value: value_to_broadcast}) }}"></iframe>
	{% endif %}
{% endblock %}
